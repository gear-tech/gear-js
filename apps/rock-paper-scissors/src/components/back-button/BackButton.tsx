import { Button } from '@gear-js/ui';
import back from 'assets/images/icons/back.svg';

function BackButton() {
  return <Button icon={back} text="Back" color="light" size="large" />;
}

export { BackButton };
