import { Button } from '@gear-js/ui';
import { Content } from '../../content';

function Start() {
  return (
    <Content subheading="Press 'Start' to set the lottery options">
      <Button text="Start" />
    </Content>
  );
}

export { Start };
