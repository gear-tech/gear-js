export enum TBErrorMessage {
    ACCESS_DENIED = "Access denied",
    COMMAND_ARGUMENTS_REQUIRED = "Command arguments 😡 required",
    INVALID_COMMAND_ARGUMENTS = "Invalid command arguments 😡"
}
