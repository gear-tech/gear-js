export enum RMQQueues {
  GENESISES = 'genesises',
  REPLIES = 'replies',
}

export enum RMQExchanges {
  DIRECT_EX = 'direct.ex',
  TOPIC_EX = 'topic.ex',
}

export enum RMQServices {
  INDEXER = 'indxr',
  TEST_BALANCE = 'tb',
  META_STORAGE = 'meta',
}

export enum RMQServiceActions {
  ADD = 'add',
  DELETE = 'delete',
}
