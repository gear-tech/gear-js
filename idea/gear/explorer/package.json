{
  "name": "gear-idea-explorer",
  "version": "1.4.26",
  "private": true,
  "type": "module",
  "dependencies": {
    "class-validator": "0.15.1",
    "cron": "4.4.0",
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "gear-idea-common": "workspace:*",
    "gear-idea-indexer-db": "workspace:*",
    "pg": "8.20.0",
    "redis": "5.12.1",
    "reflect-metadata": "0.2.2"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^12.0.0",
    "@types/express": "5.0.6",
    "@types/pg": "^8.20.0"
  },
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "dev": "npx ts-node-dev src/main.ts",
    "start": "node dist/main.js",
    "clean": "echo \"clean gear-idea-explorer\" && rm -rf dist",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "generate-dump": "./scripts/generate-dump.sh"
  }
}
