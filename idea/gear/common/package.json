{
  "name": "gear-idea-common",
  "private": true,
  "version": "1.3.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "clean": "echo \"clean gear-idea-common\" && rm -rf dist"
  },
  "dependencies": {
    "@gear-js/api": "0.41.1",
    "@polkadot/api": "15.9.2",
    "winston": "3.17.0"
  }
}
