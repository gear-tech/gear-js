{
  "name": "gear-idea-common",
  "private": true,
  "version": "1.3.10",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "npx tsc",
    "clean": "echo \"clean gear-idea-common\" && rm -rf dist"
  },
  "dependencies": {
    "@gear-js/api": "0.41.1",
    "@polkadot/api": "15.10.2",
    "winston": "3.7.2"
  }
}
