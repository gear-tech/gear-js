{
  "name": "gear-idea-common",
  "private": true,
  "version": "1.3.11",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "npx tsc",
    "clean": "echo \"clean gear-idea-common\" && rm -rf dist"
  },
  "dependencies": {
    "@gear-js/api": "0.42.0",
    "@polkadot/api": "16.0.1",
    "winston": "3.7.2"
  }
}
