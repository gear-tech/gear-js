const METHOD = {
  TO_PROGRAM: 'messageToProgram.data',
  FROM_PROGRAM: 'messageFromProgram.data',
  TO_PROGRAM_ALL: 'messageToProgram.all',
  FROM_PROGRAM_ALL: 'messageFromProgram.all',
} as const;

export { METHOD };
