name: 'Publish @gear-js/ui'

on:
  push:
    branches: [master]
    paths:
      - utils/gear-ui/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Use node 16.x
        uses: actions/setup-node@v2.1.5
        with:
          node-version: 16.x

      - name: Check package version
        uses: EndBug/version-check@v1
        id: check
        with:
          file-name: utils/gear-ui/package.json
          file-url: https://unpkg.com/@gear-js/ui@latest/package.json
          static-checking: localIsNew

      - name: Publish
        if: steps.check.outputs.changed == 'true'
        working-directory: utils/gear-ui
        run: |
          export token=$(printenv $(printenv GITHUB_ACTOR))
          npm ci
          npm config set //registry.npmjs.org/:_authToken=$token
          npm publish
        env:
          osipov-mit.NPM_TOKEN: ${{ secrets.OSIPOV_NPM_TOKEN }}
          nikitayutanov.NPM_TOKEN: ${{ secrets.YUTANOV_NPM_TOKEN }}
